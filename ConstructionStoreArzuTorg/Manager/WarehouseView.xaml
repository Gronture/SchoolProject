<Window x:Class="ConstructionStoreArzuTorg.Manager.WarehouseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionStoreArzuTorg.Manager"
        mc:Ignorable="d"
        Height="300" Width="460">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ScrollViewer.HorizontalScrollBarVisibility="Disabled" IsReadOnly="True" x:Name="grid" FontFamily="Times New Roman" Margin="140,0,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding НазваниеТовара}" Header="Товар" Width="80" FontFamily="Times New Roman"/>
                <DataGridTextColumn Binding="{Binding НазваниеКатегории }" Header="Категория" Width="100" FontFamily="Times New Roman"/>
                <DataGridTextColumn Binding="{Binding Количество}" Header="Количество" Width="100" FontFamily="Times New Roman"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="BackButton" Content="Назад" HorizontalAlignment="Left" Margin="30,200,0,0" VerticalAlignment="Top" Click="BackButton_Click" Width="96" Height="32"/>
    </Grid>
</Window>
